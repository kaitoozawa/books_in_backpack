<div>
  <h1 class="request_books_title"><%= @counts %> Request</h1>
  <ul>
    <% @request_users.each do |user| %>
      <li><%= link_to user.profile.name, profile_path(id: user.profile.id) %> :  <%= user.book.title %></li>
    <% end %>
  </ul>
</div>
<div>
  <%= link_to 'Back to Search', search_path %>
</div>